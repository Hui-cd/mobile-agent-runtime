<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".debug.RendererTestReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="ai.mobileagent.DEBUG_TERMINATE_RENDERER" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".debug.ModelDelayTestReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="ai.mobileagent.DEBUG_DELAY_NEXT_MODEL" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".debug.QuickJsFixtureTestReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="ai.mobileagent.DEBUG_RUN_QUICKJS_FIXTURE" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".debug.AgentTaskTestReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="ai.mobileagent.DEBUG_RUN_AGENT_TASK" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
